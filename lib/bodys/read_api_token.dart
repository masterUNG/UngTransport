import 'package:flutter/material.dart';

class ReadApiToken extends StatelessWidget {
  const ReadApiToken({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This Read API'),
    );
  }
}
import 'package:flutter/material.dart';

class GetLocationShowMap extends StatelessWidget {
  const GetLocationShowMap({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is GetLocation'),
    );
  }
}